<section class="about-description" aria-label="Descripción de Fundación SENO">
  <p>
    Somos una fundación sin fines de lucro creada en Neuquén Capital. El 4 de
    octubre de 2010 obtuvimos la
    <strong> Personería Jurídica, Decreto N.º 1893</strong>. Nuestro trabajo se
    centra en la protección y el acompañamiento de niños, niñas y adolescentes
    con cáncer o tumores.
  </p>
</section>

<style>
  .about-description {
    width: min(100%, 56.25rem);

    margin-inline: auto;

    padding: clamp(1.25rem, 4vw, 2rem);

    color: var(--color-text);

    background-color: rgba(192, 226, 156, 0.35);

    background-color: color-mix(
      in srgb,
      var(--color-primary-light) 35%,
      transparent
    );

    border-left: 4px solid var(--color-primary);

    border-radius: 0 var(--radius) var(--radius) 0;
  }

  .about-description p {
    font-size: clamp(1rem, 2.5vw, 1.15rem);

    line-height: 1.8;
    text-wrap: pretty;
    overflow-wrap: anywhere;
  }

  .about-description strong {
    color: var(--color-primary-hover);

    font-weight: 750;
  }

  @media (max-width: 500px) {
    .about-description {
      padding: 1.1rem;

      border-left-width: 3px;
    }

    .about-description p {
      font-size: 1rem;
      line-height: 1.65;
    }
  }
</style>
