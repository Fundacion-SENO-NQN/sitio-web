<p class="text">
  Somos una fundación sin fines de lucro creada en Neuquén Capital. El 4 de
  octubre de 2010 obtuvimos la
  <strong> Personería Jurídica, Decreto N.º 1893</strong>. Nuestro trabajo se
  centra en la protección y el acompañamiento de niños, niñas y adolescentes con
  cáncer o tumores.
</p>

<style>
  .text {
    box-sizing: border-box;

    width: min(100%, 900px);
    margin: 0 auto;
    padding: clamp(1.25rem, 4vw, 2rem);

    border-left: 4px solid var(--color-primary);
    border-radius: 0 1rem 1rem 0;
    background-color: color-mix(
      in srgb,
      var(--color-primary-light) 35%,
      transparent
    );

    color: var(--color-text);

    font-family: var(--text-font-primary);
    font-size: clamp(1rem, 2.5vw, 1.15rem);
    line-height: 1.8;
    overflow-wrap: anywhere;
  }

  strong {
    color: var(--color-primary-hover);
  }

  @media (max-width: 500px) {
    .text {
      padding: 1.1rem;

      border-left-width: 3px;

      font-size: 1rem;
      line-height: 1.65;
    }
  }
</style>
