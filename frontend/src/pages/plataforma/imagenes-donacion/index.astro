---
import HeaderPlataforma from '../../../components/Plataforma/HeaderPlataforma.astro'
import ModalCarga from '../../../components/Common/ModalCarga.astro'
import LayoutPlataforma from '../../../layouts/LayoutPlataforma.astro'

import '../../../style/uploadImages.css'
---

<LayoutPlataforma titulo="Imágenes de donación">
  <ModalCarga />

  <HeaderPlataforma
    title="Imágenes de donación"
    content="Actualizá las fotografías de donaciones que se muestran en el sitio web."
  />

  <main class="adminPage upload-page">
    <section class="uploadCard" aria-labelledby="upload-title">
      <header class="uploadCardHeader">
        <h2 id="upload-title">Subir nuevas imágenes</h2>

        <p id="upload-description">
          Podés seleccionar hasta 10 fotografías. Cada imagen reemplazará,
          individualmente, la fotografía de donación más antigua.
        </p>
      </header>

      <form id="uploadForm" class="uploadForm" enctype="multipart/form-data">
        <label id="dropZone" class="imagePicker drop-zone" for="images">
          <input
            id="images"
            class="srOnly"
            name="images"
            type="file"
            accept="image/jpeg,image/png,image/webp,image/avif"
            aria-describedby="upload-description upload-formats"
            multiple
            required
          />

          <span class="imagePickerIcon icon" aria-hidden="true"> 🖼️ </span>

          <strong> Soltá las imágenes acá </strong>

          <span> O hacé clic para seleccionarlas </span>

          <small id="upload-formats">
            Hasta 10 imágenes JPG, PNG, WebP o AVIF. Máximo 12 MB por archivo.
          </small>
        </label>

        <div
          id="previewContainer"
          class="imagePreviewContainer previewContainer hidden"
          aria-label="Vista previa de las imágenes seleccionadas"
          aria-live="polite"
        >
        </div>

        <fieldset class="instagramPanel">
          <legend>Instagram</legend>
          <p id="instagramStatus" role="status">Consultando conexión...</p>
          <p id="instagramAccount">Cuenta: —</p>
          <div class="instagramButtons">
            <button
              id="instagramConnect"
              type="button"
              class="button buttonGhost"
              disabled>Conectar Instagram</button
            >
            <button
              id="instagramDisconnect"
              type="button"
              class="button buttonGhost"
              hidden>Desconectar</button
            >
          </div>
          <label
            ><input id="publishInstagram" type="checkbox" disabled /> Publicar estas
            imágenes en Instagram</label
          >
          <label
            ><input id="instagramComments" type="checkbox" checked disabled /> Habilitar
            comentarios en Instagram</label
          >
        </fieldset>

        <div class="instagramCaption">
          <label for="donationTitle"
            >Título <span id="titleCount">0/120</span></label
          >
          <input id="donationTitle" maxlength="120" />
          <label for="donationDescription"
            >Descripción <span id="descriptionCount">0/1200</span></label
          >
          <textarea id="donationDescription" maxlength="1200" rows="6"
          ></textarea>
          <small
            >Instagram mostrará el título y la descripción juntos. Seleccioná de
            1 a 10 imágenes para una foto o un carrusel.</small
          >
        </div>

        <div class="uploadActions">
          <button
            id="btnClearImages"
            class="button buttonGhost"
            type="button"
            disabled
          >
            Limpiar selección
          </button>

          <button
            id="btnUpload"
            class="button buttonPrimary primaryButton"
            type="submit"
            disabled
          >
            Subir imágenes
          </button>
        </div>
      </form>
    </section>

    <aside class="infoMessage uploadNotice">
      <strong>Tené en cuenta</strong>

      <p>
        Las imágenes del sitio se guardan en AVIF. Si elegís Instagram, se crea
        también un JPEG para cada foto y se publica una sola entrada o carrusel.
      </p>
    </aside>
  </main>

  <script>
    import '../../../js/uploadImages/uploadImages.js'
  </script>
</LayoutPlataforma>
